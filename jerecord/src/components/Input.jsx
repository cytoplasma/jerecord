import React from "react";

const Input = () => {
  return (
    <div className="input">
      <input type="text" placeholder="Type your message" />
      <button>↑ </button>
    </div>
  );
};

export default Input;
